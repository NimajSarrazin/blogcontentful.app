Erreur non résolu : 

GET pour l'api de google : 
- Quand il y a le addblock activé, je pense que l'erreur sera récurent si je ne met pas de token.

Erreur  résolu : 
fix : Key props, aria-controle , stroke-width sur les SVG, id sur les imputs.